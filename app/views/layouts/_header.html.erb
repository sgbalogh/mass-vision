<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#user-util-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "img.sgbalogh", images_path, id: "logo" %>
    </div>
    <div class="collapse navbar-collapse" id="user-util-collapse">
      <div class="navbar-right">
        <ul class="nav navbar-nav">
          <li><%= link_to 'Login', user_session_path unless user_signed_in? %></li>
          <li><%= link_to current_user.email, current_user unless !user_signed_in? %></li>
          <li><%= link_to 'Logout', destroy_user_session_path, method: :delete unless !user_signed_in? %></li>
        </ul>
      </div>
    </div>
  </div>
</nav>